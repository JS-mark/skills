# GitHub token for PR submissions
GITHUB_TOKEN=

# Vercel KV (for ratings, downloads, favorites)
KV_REST_API_URL=
KV_REST_API_TOKEN=

# App URL (for CORS and callbacks)
NUXT_PUBLIC_APP_URL=http://localhost:3000
